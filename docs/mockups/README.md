# V2 Mockup — locked visual reference

This folder is the **locked visual reference** for Skool Success OS.

## Files

| File                                | What it is                                             |
| ----------------------------------- | ------------------------------------------------------ |
| `skool-success-mockup-v2.html`      | **LOCKED.** The reference HTML mockup. Open in browser. |
| `skool-success-mockup.html`         | **REJECTED v1.** Archived as “what not to do.” Do not use as visual spec. |
| `01-today.png` … `07-weekly-snapshot.png` | Screenshots of the locked V2 mockup, one per top-level view. |

## Status

The HTML files (`skool-success-mockup-v2.html`,
`skool-success-mockup.html`) live in the original Claude.ai chat where
they were produced and **need to be dropped into this folder before
Day 4** (Drop-Off Map UI build). Until then, the screenshots in this
folder are the source of truth for tokens, layout, and copy.

If anything in code starts to drift from the screenshots / HTML, stop
and reconcile here first — the mockup wins.

## Design tokens (mirrored from master plan, Part 6)

- Background: `#fafaf7` (warm off-white)
- Skool-inspired highlight: `#fff9e8` (cream)
- Accent (terracotta, softer): `#d97757`
- Positive: forest green for completion %, healthy retention
- Headlines: Fraunces serif, lowercase, conversational
- Body: system stack, sized for readability not density

## Language locks (do not violate, anywhere in the app)

| Don’t say         | Say                            |
| ----------------- | ------------------------------ |
| Command Center    | Today                          |
| At-Risk Members   | Member Check-ins               |
| Active Users      | Regulars This Week             |
| Engagement Rate   | Posts that landed              |
| Churn Rate        | Retention this quarter         |
| Dashboard         | (just don’t use this word)     |
