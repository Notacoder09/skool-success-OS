import { ComingSoon } from "@/components/ComingSoon";

export default function CheckInsPage() {
  return (
    <ComingSoon
      feature="Member Check-ins"
      arrives="Arrives Days 8-10"
      description="A daily list of 5-7 members worth a DM today, ranked by relationship value, not just risk. Each row has a Draft Message button that copies a draft to your clipboard and opens the Skool DM tab."
    />
  );
}
