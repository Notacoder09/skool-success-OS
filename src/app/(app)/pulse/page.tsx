import { ComingSoon } from "@/components/ComingSoon";

export default function PulsePage() {
  return (
    <ComingSoon
      feature="Community Pulse"
      arrives="Arrives Days 8-10"
      description="What's happening in your community right now. Daily activity patterns, who the regulars are, when your members are on. Light, scannable, action-oriented — no vanity metrics."
    />
  );
}
